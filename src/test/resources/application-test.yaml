spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: false
  sql:
    init:
      mode: never
notification:
  sqs:
    queue-name: notification-events
